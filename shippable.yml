# Build Environment
build_environment: silviuburceadev/docker-ubuntu-java-maven

# language setting
language: java

# jdk tag
jdk:
 - openjdk7
 
build:
   ci:
    - mvn clean install
   post_ci:
     on_success:
      - docker build -t myImage:tip .
