deployment:
  hub:
    branch: master
    commands:
    - mkdir ${CIRCLE_ARTIFACTS}/war
    - cp -R ./target/*.war ${CIRCLE_ARTIFACTS}/war/
    - curl -H "Content-Type: application/json" --data '{"Dockerfile": "master"}' -X POST $DOCKER_TRIGGER1
  
