machine:
  services:
    - docker
    
test:
  post:
    #- mvn clean install
   # - docker build -t pushpendrad/emsdockerhub .
   # - docker build -t pushpendrad/emsdockerhub:$CIRCLE_BUILD_NUM .
    - docker build -t pushpendrad/emsdockerhub .

    
deployment:
  hub:
    branch: master
    commands:
    - mkdir ${CIRCLE_ARTIFACTS}/war
    - cp -R ./target/*.war ${CIRCLE_ARTIFACTS}/war/
    - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
    # - docker push pushpendrad/ems_deployimage:$CIRCLE_SHA1
    
    - docker tag pushpendrad/emsdockerhub pushpendrad/emsdockerhub:$latest
    - docker push pushpendrad/emsdockerhub:latest
    
    - docker tag pushpendrad/emsdockerhub pushpendrad/emsdockerhub:$CIRCLE_BUILD_NUM
    - docker push pushpendrad/emsdockerhub:$CIRCLE_BUILD_NUM
    
    #-docker push pushpendrad/emsdeployimage
    ##- docker build -t pushpendrad/emsdockerhub:$latest .

    
